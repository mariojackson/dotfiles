# Scale GDK application icons, but scale back 
# the font so it doesn't look unappropriate big
export GDK_SCALE=2
export GDK_DPI_SCALE=0.5

export ELM_SCALE=2
export QT_AUTO_SCREEN_SCALE_FACTOR=1 # honour DPI settings (works QT >= 5.6)
